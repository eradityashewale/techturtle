import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import SmoothScroll from "@/components/ui/SmoothScroll";
import CustomCursor from "@/components/ui/CustomCursor";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "TechTurtle — Building Digital Experiences That Scale",
  description:
    "TechTurtle empowers businesses with cutting-edge technology solutions — from AI-powered automation to enterprise-grade cloud infrastructure.",
  keywords: [
    "software development",
    "AI automation",
    "cloud infrastructure",
    "product design",
    "tech startup",
  ],
  openGraph: {
    title: "TechTurtle — Building Digital Experiences That Scale",
    description:
      "TechTurtle empowers businesses with cutting-edge technology solutions.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable}`}
    >
      <body className="bg-[#050505] text-white antialiased overflow-x-hidden">
        <SmoothScroll>
          <CustomCursor />
          {children}
        </SmoothScroll>
      </body>
    </html>
  );
}
